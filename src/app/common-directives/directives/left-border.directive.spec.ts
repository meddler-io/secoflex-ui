import { LeftBorderDirective } from './left-border.directive';

describe('LeftBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new LeftBorderDirective();
    expect(directive).toBeTruthy();
  });
});
